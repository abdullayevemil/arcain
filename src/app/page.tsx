export default function Page() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui" }}>
      <h1>Backend API is running</h1>
      <p>Use <code>/api/health</code> to test.</p>
    </main>
  );
}
